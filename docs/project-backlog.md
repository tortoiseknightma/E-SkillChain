# Portfolio Track Backlog

本文件只记录不阻塞 ECommerceSkillChain 第一版 Portfolio Track 的 P2/P3 问题。

记录问题后应立即返回当前主线。P0/P1 不应停放在这里；它们必须在当前阶段处理。

| 问题 | 影响 | 优先级 | 为何不阻塞当前主线 | 未来触发条件 |
|---|---|---|---|---|
| 将 Portfolio permission receipt/catalog SHA 纳入通用 `AssistantMatrixPlan` schema，并视需要生成 metadata-only assignments v2 | 可让未来不同数据集的五配置计划在 schema 层直接锁定运行时 catalog；当前 plan 只在逐行 execution receipt 中记录 catalog SHA | P2 | 第一版已有 fresh runtime preflight handle，可由 production orchestrator 在运行前强制消费并在运行后逐行验证五配置使用同一 SHA；重绑已接受 Phase 3 工件没有运行收益 | 第一版结果交付后进入第二版，或同一 orchestrator 需要支持多个可切换 catalog |
| 收敛 Portfolio verified loader 的重复深验证与跨进程 native-memory 开销 | Core 1,022 资产的重复解码会放大 Pillow native heap；同进程 loader 已从约 8 次 catalog scan 收敛为 base/runtime 各 1 次并向三 processor 与 query binding 复用 exact verified objects，但 monitor 和 matrix 独立进程仍各做一次完整深验并可保留较高 private bytes | P2 | fail-closed 信任边界、catalog SHA/pHash 与 clean Gate 0 canary 均已验证；跨进程残留未再引发 MemoryError，不应为当前收口弱化深验 | 启动 core1500、并发度高于 2、再次 MemoryError 或内存水位不足时，优先改为短生命 preflight 子进程：绑定 receipt 后立即退出回收 native heap，而不是引入无界全局 cache |
| 为 Portfolio Stage 1 handoff 增加真实数据的 opt-in integration 回归 | 单元测试使用严格 fake handle 验证契约与篡改拒绝，真实 200-query/图片/checklist/authoring input 已手动完整重建复验，但默认 CI 不自动重复这条慢路径 | P2 | 真实产物已由当前 loader 成功复验，Formal Stage 1 回归保持绿色；把本地 ignored 大工件设为默认测试前置会降低可移植性 | 有可复用的 Portfolio 数据 CI runner，或 handoff 输入/权限/catalog revision 发生变化 |
| 将 S2/S3 development gate 从每 capability 单样本升级为跨样本聚合与配对复验 | 当前六 query gate 足以验证可执行的接受/回滚机制，但不能稳定区分 Skill 级缺陷、Assistant 采样波动与 Judge 波动；与论文每 Skill 至少 30/50 样本的设置有明显差距 | P2 | 第一版只要求 S2/S3 可运行原型，且独立 175-query evaluation 才承担 headline 结论；当前 gate 已 fail-close，不会把回滚候选发布为正式 Bank | 第一版结果交付后进入第二版，或准备开展第 1 轮有记录的 S2/S3 算法优化时，预注册样本量、tier 聚合、paired common-route 与重复判定规则 |
| 校准 Final Judge 的 no-op 方差并评估冻结 structured-output 合约 | v16 的 S1 单条 Judge 连续格式失败使官方均值下降 3.40 pp（相对仅诊断补 wrapper），相同 Bank/route 的 S1+S2 与 Full 仍有 -1.28 pp 差异；25-query 阶段排序对 evaluator noise 敏感 | P2 | 当前严格 parser、两次有界重试、非计分分类和全分母计分均按冻结规则正确执行；evaluation175 已完成但配对 CI 跨 0，且后续 S1 replay 在前置 GCS Gate 回滚，Final Judge 未启动 | 进入第二版或冻结下一轮 Judge 合约前，预注册 no-op 重复、温度/structured-output 选择和基于噪声的最小 gate margin |
| 为 Portfolio execution root 增加跨进程 owner lease | 两个 operator 若对同一 execution root 并发启动 matrix/shard，可能重复产生远端费用；create-only checkpoint 会阻止静默覆盖，但不能阻止重复调用 | P2 | 第一版由单一受监督 matrix 串行执行；启动前确认同 execution root 无存活进程，进程存活时禁止 resume，并使用 fresh execution root | 再次出现同 execution root 并发启动，或需要多 operator/无人值守恢复时 |
| 为每次远端调用增加 write-ahead intent 与 orphan 恢复协议 | 进程若在 provider 响应后、checkpoint 前退出，调用费用与响应均可能没有 durable receipt；v10 曾出现一次无法精确计价的 Judge orphan | P2 | 当前对该次 orphan 单独保留 CNY 2 预算并诚实披露；后续不洁退出先停止自动恢复和人工盘点，不把未知调用伪装成已观测成本 | 再出现第二次 orphan，或开始无人值守长矩阵时升级为 P1 |
| 将 `text_product_search` 从精确标识符回退升级为真实 lexical/semantic 检索 | 当前公开数据 adapter 只能在冻结 query 中匹配商品标识符，不能按自然语言语义或风格检索 | P2 | 第一版模型侧契约会明确其能力边界并由 runner 绑定权威 query，消除配置差异化接口陷阱；现有清洗数据缺少足够标题/描述索引 | 获得审核后的商品标题/描述语料并能冻结索引、数据锁和服务实现时 |
| 用 Polyvore 原生 outfit co-occurrence 升级跨品类 Style coordination evidence | 可提供比 Codex 复核可见属性与 deterministic palette rule 更强的真实同套搭配信号，并扩展鞋、包、首饰之外的候选类别；同时会引入跨数据集实体映射、source lock、许可边界和 leakage 审计成本 | P2 | 当前 query-independent curated graph 已为 Core 的 `footwear / bag / jewelry` 请求提供受绑定候选，并明确不声称 outfit 共现；Polyvore 集成不影响当前工具闭环和诚实结论 | 当前支持类别覆盖或 blind preference 成为主要瓶颈，且 Core headline 已冻结后；启动前预注册 FashionIQ→Polyvore 映射、component 隔离、source receipt 与增量消融 |
| 修正 Core launch 预检中的历史 `Kimi Feedback` 展示文案为当前 `Qwen3.7 Feedback` | 当前 warning 文案与实际 `feedback_provider`、processor order 和权限绑定不一致，可能在人工阅读回执时造成困惑 | P2 | 本轮 S1 Feedback 的真实 Qwen 运行身份、权限、预算与结果绑定均正确；该 warning 不参与 provider 选择、权限验证或 Gate 判定 | 第一版结果交付后整理公开展示材料时一并改为由 active role spec 动态生成并补文案回归 |
| 收敛 legacy Portfolio runner 的 Static-parent S2 入口 | 旧 S2 attribution 仍固定读取 `source_config=s1` 的 `dm-001..025` development smoke；Core Fast 已能在 S1 rollback 后从 byte-exact Static alias 继续 S2，但 legacy runner 尚未采用该父 Bank 语义 | P2 | 当前默认 Core Fast 路径已通过 Static-parent S2 聚焦回归并提供同一 output root 的 resume 命令，不阻塞本轮交接；回写旧 runner 会扩大当前纵切 | 需要维护 legacy Formal/旧 Portfolio runner，或把 Core Fast 的 typed attribution、runtime 与 route gate 反向移植时 |
| 在下一版 Assistant runtime preflight 中校验商品候选与卡片闭包 | 当前响应预检主要校验 section/repair 合约；模型可能在工具已返回 eligible product 时仍输出 `no supported match` 与 `product_cards: none`，直到下游 GCS 才以 `card_contract_failed` 拒绝 | P2 | 冻结 GCS 已正确拦截 R3 候选并触发回滚，指标和最终选择不受损；现在修改 runtime 会造成冻结 Static 与新候选执行不对称，不能据此重跑或追认 R3 | 当前五配置实验冻结后、创建新版 runtime contract 前，版本化该校验并用 paired no-op baseline 验证不引入比较偏差 |
| 验证确定性 action/response contract 的新 lineage 增益 | `core-fast-deterministic-action-response-v1` 已把 routed config 的固定工具序列、DTO/card/evidence closure 和 fallback 互斥迁入 runner/compiler；历史 runner、NoSkill、GCS 与旧 artifacts 隔离 | P2 | 本次只完成代码与离线回归，未授权新的付费 Static/S1 比较；不能用实现完成替代效果证据，也不能追认旧候选 | 第一版结果交付后如另行授权，以新 output root 做对称 Static/S1 对比；保持现有 screen/gate，不读取 val/test 调参 |
