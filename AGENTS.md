<!-- ARIS:BEGIN -->
## ARIS Skill Scope
ARIS skills installed in this project: 80 entries.
Manifest: `.aris/installed-skills.txt` (lists every skill ARIS installed and its upstream target).
For ARIS workflows, prefer the project-local skills under `.Codex/skills/` over global skills.
Do not modify or delete files inside any skill that is a symlink (symlinks point into `/mnt/d/athena/aris_repo`).
Update with: `bash /mnt/d/athena/aris_repo/tools/install_aris.sh`  (re-runnable; reconciles new/removed skills).
<!-- ARIS:END -->

# ECommerceSkillChain 项目执行原则

## 1. 项目初心与优先级

本项目首先是一项面向 Agent/算法工程岗位的求职作品，其次才是研究报告和长期研究框架。项目希望让面试官记住：

> 我面向真实业务场景实现了基于 Skill 优化的自进化闭环，同时具备算法优化能力和工程实现能力。

这里的“真实业务场景”指真实电商任务语义、公开商品数据、真实模型调用和真实工具执行；不声称复现企业生产流量、内部数据、专有工具、线上用户分布或论文绝对分数。

工作优先级固定为：

1. 跑通可信的端到端实验；
2. 形成可展示、可讲述的求职项目成果；
3. 整理研究报告；
4. 完善通用研究框架。

当目标冲突时，执行优先级更高的目标。研究严谨性用于保证比较公平、结果真实和声明诚实，不得演变为无限扩张的审计工程。

## 2. 两条工作轨道

### Portfolio Track（默认主线）

Portfolio Track 服务求职展示。它要求实验真实运行、比较公平、结果诚实、主要结论有数据支持，但不要求在实验前完成学术发表级审计。除非用户明确指定，Codex始终推进此轨道。

### Formal Research Track（非默认）

Formal Research Track 用于形成更严格的研究结论，继续受以下文件中的冻结、审计、许可、人工校准和 go/no-go 规则约束：

- `docs/reproduction-contract.md`
- `docs/evaluation-protocol.md`
- `docs/go-no-go/`
- `docs/plans/`

Formal Research Track 的 NO-GO 不得自动阻塞 Portfolio Track。发生冲突时：

- Portfolio Track 的范围、优先级和执行顺序以本文件为准；
- formal eligibility、正式研究命名和发表级声明仍以研究契约及评价协议为准。

## 3. 第一版交付目标

第一版保持当前完整实验范围，不采用“先窄后宽”：

- 覆盖全部 6 个现有 capability；
- 运行全部 5 个主配置：`NoSkill / LLMStaticSkill / S1 / S1+S2 / Full`；
- 完成明确的 `dev_mini=200` 端到端实验；
- 让 S1 Creator 真正形成自进化闭环；
- 让 S2 Route Optimizer 和 S3 Body Refiner 达到可运行原型；
- 完成量化比较、案例分析和求职展示材料。

第一版暂不要求运行 `core=1,500`。完成 200-query mini 和求职成果整理后，再决定是否扩量。

### S1 完整闭环

第一版必须真实跑通：

```text
运行初始系统
→ 收集和归因失败轨迹
→ S1 根据轨迹创建或改进 Skill Bank
→ 使用更新后的 Skill Bank 重新运行
→ 按统一规则评测
→ 接受改进或回滚
```

闭环不能只有接口、fixture 或文档；必须包含真实模型调用、真实工具执行、落盘产物和可复核的前后对比。

### S2/S3 第一版标准

S2 和 S3 的完整闭环属于第二版。第一版中，它们至少应当：

- 有真实可执行代码；
- 能读取预期的轨迹或 attribution 输入；
- 能生成合法候选 Skill；
- 能执行一次接受或回滚判断；
- 具有聚焦测试和可复现 smoke run。

第一版不要求 S2、S3 分别证明统计显著增益，也不应为其建设 formal 级审计体系。

## 4. 第一版完成条件

同时满足以下条件，即视为第一版完成：

- 200-query mini 覆盖全部现有 capability；
- 五个主配置均已真实运行；
- S1 至少完成一次真实迭代；
- S2 和 S3 达到可运行原型标准；
- 产生统一指标下的五配置量化比较；
- 保存有代表性的成功、失败和 Skill 改进案例；
- 实验可通过一条或少量命令重新执行；
- 模型、工具、调用成本、已知限制和数据替代得到诚实披露；
- 形成结果表、关键图表、系统架构说明、2–3 个案例以及 30 秒/2 分钟面试介绍；
- 未解决但不阻塞的问题已进入统一 backlog。

达到这些条件后，Codex必须停止扩建基础设施，先交付实验结果和求职成果。暂不要求可交互 Demo 或录屏。

## 5. 效果不佳时的停止规则

工程闭环完成与算法取得正向效果是两个不同结论。第一版仍应争取让至少一个包含演化的配置，在预先确定的主要指标或明确报告的 capability 分析上优于 `LLMStaticSkill`。

如果没有得到正向提升：

1. 检查实现、数据流、评测和明显算法问题；
2. 最多进行三轮有记录的算法诊断与优化；
3. 不得修改测试标签、利用保留集调参、隐藏失败样本或为追求好看结果改写评分规则；
4. 三轮后无论结果如何都停止优化；
5. 如实交付负结果、原因分析和下一步建议。

不得为了取得正向结果而无限迭代。

## 6. 问题优先级

- **P0**：使实验无法运行、指标无效、比较不公平或核心结论明显错误。立即修复。
- **P1**：明显削弱端到端演示、核心可复现性或面试可信度。当前阶段修复。
- **P2**：有实际价值，但不阻塞第一版实验或成果展示。记录到 backlog，然后继续主线。
- **P3**：理论完善、额外泛化、形式优化或通用框架建设。除非用户明确要求，否则不做。

对于 P2/P3，Codex不得以“顺手完善”为由扩大任务。能够通过排除问题数据、缩短非核心实现或明确披露限制解决的问题，不应升级成新的治理或基础设施项目。

## 7. 非主线治理工作

Portfolio Track 不以完成许可证、PII、source lock、provenance、外部 trust root 或发表级审计体系作为实验前置条件。

- 不主动扩建上述治理体系；
- 不因 formal gate 未关闭而停止 Portfolio Track；
- 已有相关代码和文档可以保留，但不得继续主导执行顺序；
- 若发现明显凭据、私人信息或不能安全使用的内容，直接排除；
- 不为排除个别问题启动新的治理子项目；
- 对公开展示所使用的数据和限制保持诚实说明。

## 8. Codex自主执行边界

在既定范围内，Codex可以自主：

- 修改代码、测试和必要文档；
- 选择满足要求的最小可行实现；
- 修复 P0/P1；
- 把 P2/P3 写入 `docs/project-backlog.md`；
- 运行本地测试、smoke test 和实验；
- 在预算内调用模型；
- 根据实验结果进行不超过规定轮数的算法优化；
- 在阶段结束后集中汇报。

以下事项需要事先征求用户：

- 改变六个 capability、五个主配置或主要实验比较；
- 改变项目主要主张；
- 扩大到 core/full profile；
- 超过自主模型调用预算；
- 发布外部内容或产生其他明显外部影响；
- 删除重要数据、改写历史实验记录或执行不可恢复操作；
- 降低已经确定的基本比较公平性。

## 9. 模型调用预算

相邻两次阶段汇报之间，Codex可以自主使用：

- DashScope：累计预计费用不超过人民币 10 元；
- Codex：不超过 100 次新的实验会话。

每次调用必须直接服务当前实验，并记录模型、用途、调用次数、结果和可得的费用信息。失败调用计入预算；不得无界重试或通过重复调用挑选最好结果。预计超过任一额度时，先征求用户同意。

## 10. 防止偏离主线

开始非例行工作前，Codex必须能够说明它直接关闭哪个第一版完成条件或哪个 P0/P1。若不能：

1. 将问题写入 `docs/project-backlog.md`；
2. 记录影响、优先级、不阻塞理由和未来触发条件；
3. 返回当前主线。

默认禁止：

- 为已有证明继续建设“证明的证明”；
- 为一次性实验提前抽象通用框架；
- 创建不直接推动第一版交付的新 gate、receipt、lock 或审计层；
- 因无关测试、历史环境漂移或 formal replay 问题阻塞当前纵切；
- 在多个文档重复维护相同状态；
- 用长篇设计文档替代可运行实现；
- 为非关键边界条件无限增加测试。

测试强度应与风险相称：先运行聚焦测试，再运行相关回归；无关历史失败进入 backlog，不得自动升级为主线任务。

## 11. 阶段汇报与求职成果

Codex在阶段内自主工作，在阶段结束后集中汇报：

- 已完成工作及其对应的第一版完成条件；
- 实验指标和代表性案例；
- 模型调用次数和费用；
- 已修复的 P0/P1；
- 新增的 P2/P3；
- 已知限制和下一阶段建议。

除非出现需要用户批准的事项，不因普通实现选择频繁中断用户。

第一版实验完成后，优先生成：

- 简洁的项目定位与贡献说明；
- 可复现运行入口；
- 五配置结果表和关键图表；
- 自进化闭环架构图；
- 2–3 个有代表性的 Skill 改进案例；
- 失败案例和限制说明；
- 30 秒与 2 分钟项目介绍；
- 面向面试追问的算法、实验和工程取舍说明。

不得把公开数据机制级复现描述成企业生产系统，也不得把 portfolio/exploratory 结果描述成论文正式复现结论。

## 12. 第二版计划

只有第一版实验和求职成果已经交付后，才启动第二版。第二版的主要目标是：

- 完成 S2 Route Optimizer 的真实接受/回滚闭环；
- 完成 S3 Body Refiner 的真实接受/回滚闭环；
- 比较各阶段独立贡献；
- 根据第一版结果决定是否扩展到 core profile。

第二版不得反向阻塞第一版交付。

## 13. Interview-oriented project memory

仅在遇到确有面试价值的非平凡工程问题时，更新 `docs/interview-project-lessons.md`。不要为例行修改、普通 bug 或形式完善新增条目。

记录应面向项目所有者，区分已验证事实、推断和待完成工作，并说明背景与影响、可观察证据、根因、方案取舍、最终选择、验证、剩余限制、30 秒/2 分钟回答及相关文件/测试/产物/提交。不得记录凭据、私人数据或无必要的敏感信息。
